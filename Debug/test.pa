program HelloWorld;
    const
        a = 3;
        b = 2.;
        c = 3.4;
        d = true;
        
    var
        e, f, g : (* yes, they are integers *) integer;
        h, i : real;

    procedure ProcedureWithNoParams ;
        const
            a = 3;
            b = 2.;
            c = 3.4;
            d = true;
        var
            f, g : integer;
            h, i : real;
    begin
        while (e mod 3 <> 2) do
        begin
            e := a + 2;
            if e <= 3 then ProcedureWithNoParams
        end;
    end;
    
    procedure ProcedureWithParams(amunzora, qbard, alit : integer; bpoint, cpoint, apoint : real);
        const
            q = 3;
        var
            g: integer;
    begin
        g := ((4 * q) + (a div 2) * alit) mod 15;
        ProcedureWithNoParams;
        ProcedureWithParams(a, a + 1, qbard * amunzora - alit * 2, e, f, h)
    end;
begin
    h:=3;
    { q:= q + 2 }
end.
